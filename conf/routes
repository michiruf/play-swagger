# This file defines all module routes (Higher priority routes first)
#
# import these routes in the main app like this:
# *     /api                    module:swagger
#
# ~~~~

GET     /swagger.json           swagger.SwaggerController.json
